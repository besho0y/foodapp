import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const Color primaryLight = Color(0xFFFF6F00); // Orange
  static const Color accentLight = Color(0xFFFF5722); // Deep Orange

  // Dark Theme Colors
  static const Color primaryDark = Color(0xFFFF8F00); // Softer Orange
  static const Color accentDark = Color(0xFF00BFA5); // Teal

  // Common Colors
  static const Color lightBackground = Color(0xFFFAFAFA);
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkCard = Color(0xFF1F1F1F);
}
